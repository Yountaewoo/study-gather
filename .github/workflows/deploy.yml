name: Deploy to EC2 on Push

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SSH Key  # 대안 액션 사용
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.EC2_SSH_KEY }}
          known_hosts: unnecessary  # 호스트 자동 추가

      - name: Add host to known_hosts  # 필요 시 추가
        run: ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no $$   {{ secrets.EC2_USER }}@   $${{ secrets.EC2_HOST }} "
            cd ${{ secrets.PROJECT_DIR }} &&
            git pull origin main &&
            chmod +x gradlew &&
            ./gradlew clean build &&
            pkill java || true &&
            nohup java -jar build/libs/study-gather-0.0.1-SNAPSHOT.jar > app.log 2>&1 &
          "
